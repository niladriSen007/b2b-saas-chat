version: "3.9"

services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env   # (optional) load from .env file instead of hardcoding
    restart: unless-stopped
